export const VERSION = '0.6.0';
// 3: findings are keyed by route and carry `crossings` instead of a single
// challenger, plus a `confidence` of `low`. `explain` still reads 1 and 2.
export const REPORT_SCHEMA_VERSION = 3;
