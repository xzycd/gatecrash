version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: monthly
    groups:
      development:
        dependency-type: development
    ignore:
      # typescript-eslint 8.65 supports TypeScript below 6.1.
      - dependency-name: typescript
        update-types: ["version-update:semver-major"]
